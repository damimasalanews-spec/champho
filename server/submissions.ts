import { createHash, randomUUID } from "node:crypto";
import type { PoolClient } from "pg";
import { pool } from "./db.js";
import type { RoomEvent, RoomSnapshot } from "./rooms.js";
const SOLVE_WINDOW_MS=3000;const LETTERS="AAAAAAAAABBCCCDDEEEEEEEEEEEFFGGGHHHHIIIIIJKLLLLMNNNNNOOOOOOOOPPQQRRRRRSSSSSTTTTTTUUUUVVWWXYYZ";
type SubmissionCard={cardId:string;value:string};export type WordSubmissionResult={type:"word_submission_result";requestId:string;roomId:string;roundNumber:number;status:"accepted"|"rejected"|"duplicate";reason:string;serverTime:string;roundState?:"waiting"|"playing"|"solve_window"|"round_end"|"finished";cardsConsumed:number;cardsDrawn:number;handChanged:boolean;handVersion?:number;hand?:SubmissionCard[];handHash?:string;originalStatus?:"accepted"|"rejected";scoreDelta:number;coinDelta:number};export type RoundCompleted={type:"round_completed";roomId:string;roundNumber:number;roundState:"round_end";word:string;completedAt:string;solvers:Array<{playerId:string;position:number}>};export type SubmitWordInput={requestId:string;roomId:string;playerId:string;roundNumber:number;turnNumber:number;cards:string[];word:string};export type SubmitWordOutcome={result:WordSubmissionResult;events:RoomEvent[];roundCompleted?:RoundCompleted;snapshot?:RoomSnapshot};
function canonicalHash(i:SubmitWordInput){return`sha256:${createHash("sha256").update(JSON.stringify([i.roomId,i.playerId,i.roundNumber,i.turnNumber,i.cards,i.word.trim().toLowerCase()])).digest("hex")}`}function handHash(h:SubmissionCard[]){return`sha256:${createHash("sha256").update(JSON.stringify(h)).digest("hex")}`}function asHand(v:unknown):SubmissionCard[]{if(!Array.isArray(v))return[];return v.filter((c):c is SubmissionCard=>!!c&&typeof c==="object"&&typeof(c as any).cardId==="string"&&typeof(c as any).value==="string")}
function drawCards(n:number){const poolLetters=LETTERS.split("");const out:SubmissionCard[]=[];for(let i=0;i<n;i++){const j=Math.floor(Math.random()*poolLetters.length);out.push({cardId:randomUUID(),value:poolLetters.splice(j,1)[0].toLowerCase()});}return out}
async function appendEvent(client:PoolClient,roomId:string,roundNumber:number,eventType:string,payload:Record<string,unknown>):Promise<RoomEvent>{const next=await client.query(`UPDATE public.game_rooms SET event_sequence=event_sequence+1,updated_at=clock_timestamp() WHERE id=$1 RETURNING event_sequence`,[roomId]);const sequence=Number(next.rows[0].event_sequence);const inserted=await client.query(`INSERT INTO public.room_events(room_id,event_sequence,round_number,event_type,payload) VALUES($1,$2,$3,$4,$5::jsonb) RETURNING created_at`,[roomId,sequence,roundNumber,eventType,JSON.stringify(payload)]);return{type:"event",eventSequence:sequence,roomId,roundNumber,eventType:eventType as RoomEvent["eventType"],serverTime:new Date(inserted.rows[0].created_at).toISOString(),payload}}
async function readSnapshot(client:PoolClient,roomId:string):Promise<RoomSnapshot>{const room=await client.query(`SELECT id,state,phase,round_number,turn_number,active_player_id,event_sequence,first_solver_id,solved_at,solve_window_ends_at FROM public.game_rooms WHERE id=$1`,[roomId]);const players=await client.query(`SELECT player_id,seat_number,connected,score FROM public.room_players WHERE room_id=$1 ORDER BY seat_number`,[roomId]);if(room.rowCount!==1)throw new Error("room_not_found");const r=room.rows[0];return{type:"room_snapshot",roomId:r.id,eventSequence:Number(r.event_sequence),state:r.state,phase:r.phase,roundNumber:Number(r.round_number),turnNumber:Number(r.turn_number),activePlayerId:r.active_player_id,firstSolverId:r.first_solver_id,solvedAt:r.solved_at?new Date(r.solved_at).toISOString():null,solveWindowEndsAt:r.solve_window_ends_at?new Date(r.solve_window_ends_at).toISOString():null,serverTime:new Date().toISOString(),players:players.rows.map(p=>({playerId:p.player_id,seatNumber:Number(p.seat_number),connected:p.connected,score:Number(p.score)}))}}
function resultBase(i:SubmitWordInput,status:WordSubmissionResult["status"],reason:string):WordSubmissionResult{return{type:"word_submission_result",requestId:i.requestId,roomId:i.roomId,roundNumber:i.roundNumber,status,reason,serverTime:new Date().toISOString(),cardsConsumed:0,cardsDrawn:0,handChanged:false,scoreDelta:0,coinDelta:0}}
async function completeExpiredRound(client:PoolClient,roomId:string,roundNumber:number){const accepted=await client.query(`SELECT player_id,submitted_word FROM public.submissions WHERE room_id=$1 AND round_number=$2 AND status='accepted' ORDER BY processed_at,id`,[roomId,roundNumber]);const solvers=accepted.rows.map((r,i)=>({playerId:r.player_id as string,position:i+1}));const word=accepted.rows[0]?.submitted_word as string|undefined;if(!word)throw new Error("round_word_missing");const completedAt=new Date().toISOString();const roundCompleted={type:"round_completed" as const,roomId,roundNumber,roundState:"round_end" as const,word,completedAt,solvers};const event=await appendEvent(client,roomId,roundNumber,"round_completed",{word,solvers});const nextRound=roundNumber+1;const nextPlayer=await client.query(`SELECT player_id FROM public.room_players WHERE room_id=$1 AND connected=true ORDER BY seat_number LIMIT 1`,[roomId]);const activePlayerId=nextPlayer.rows[0]?.player_id??null;await client.query(`UPDATE public.game_rooms SET round_number=$2,turn_number=turn_number+1,active_player_id=$3::uuid,phase=CASE WHEN $3::uuid IS NULL THEN 'round_end' ELSE 'playing' END,state='active',first_solver_id=NULL,solved_at=NULL,solve_window_ends_at=NULL,updated_at=clock_timestamp() WHERE id=$1`,[roomId,nextRound,activePlayerId]);return{roundCompleted,events:[event]}}
export async function submitWord(input:SubmitWordInput):Promise<SubmitWordOutcome>{const client=await pool.connect();try{await client.query("BEGIN");const requestHash=canonicalHash(input);const room=await client.query(`SELECT id,state,phase,round_number,turn_number,active_player_id,solve_window_ends_at FROM public.game_rooms WHERE id=$1 FOR UPDATE`,[input.roomId]);if(room.rowCount!==1)throw new Error("room_not_found");const existing=await client.query(`SELECT status,result FROM public.submissions WHERE room_id=$1 AND player_id=$2 AND submission_id=$3`,[input.roomId,input.playerId,input.requestId]);if(existing.rowCount===1){const stored=existing.rows[0];if(stored.result?.requestHash!==requestHash)throw new Error("request_id_conflict");const{requestHash:_h,...storedResult}=stored.result;await client.query("COMMIT");return{result:{...storedResult,status:"duplicate",originalStatus:stored.status},events:[]}}
const r=room.rows[0];if(Number(r.round_number)!==input.roundNumber)throw new Error("round_mismatch");if(Number(r.turn_number)!==input.turnNumber)throw new Error("turn_mismatch");if(!["playing","solve_window"].includes(r.phase))throw new Error("room_not_playing");const player=await client.query(`SELECT player_id,private_hand,hand_version,score FROM public.room_players WHERE room_id=$1 AND player_id=$2 FOR UPDATE`,[input.roomId,input.playerId]);if(player.rowCount!==1)throw new Error("player_not_in_room");if(r.phase==="playing"&&r.active_player_id&&r.active_player_id!==input.playerId)throw new Error("not_active_player");const now=new Date();if(r.phase==="solve_window"&&new Date(r.solve_window_ends_at).getTime()<=now.getTime()){const completed=await completeExpiredRound(client,input.roomId,input.roundNumber);const snapshot=await readSnapshot(client,input.roomId);await client.query("COMMIT");return{result:{...resultBase(input,"rejected","solve_window_expired"),roundState:"round_end"},events:completed.events,roundCompleted:completed.roundCompleted,snapshot}}
const hand=asHand(player.rows[0].private_hand);const unique=new Set(input.cards);if(!input.cards.length||unique.size!==input.cards.length)throw new Error("invalid_cards");const selected=input.cards.map(id=>hand.find(c=>c.cardId===id));if(selected.some(c=>!c))throw new Error("invalid_cards");const word=input.word.trim().toLowerCase();const expected=selected.map(c=>c!.value.trim().toLowerCase()).join("");if(!word||word!==expected){const rejected={...resultBase(input,"rejected","incorrect_word"),roundState:r.phase};await client.query(`INSERT INTO public.submissions(room_id,player_id,submission_id,round_number,turn_number,submitted_cards,submitted_word,request_hash,status,reason,result) VALUES($1,$2,$3,$4,$5,$6::jsonb,$7,$8,'rejected',$9,$10::jsonb)`,[input.roomId,input.playerId,input.requestId,input.roundNumber,input.turnNumber,JSON.stringify(input.cards),input.word,requestHash,"incorrect_word",JSON.stringify({...rejected,requestHash})]);await client.query("COMMIT");return{result:rejected,events:[]}}
const replacements=drawCards(input.cards.length);const nextHand=hand.filter(c=>!unique.has(c.cardId)).concat(replacements);const nextVersion=Number(player.rows[0].hand_version)+1;await client.query(`UPDATE public.room_players SET private_hand=$3::jsonb,hand_version=$4,hand_round_number=$5,score=score+1,updated_at=clock_timestamp() WHERE room_id=$1 AND player_id=$2`,[input.roomId,input.playerId,JSON.stringify(nextHand),nextVersion,input.roundNumber]);let roundState:WordSubmissionResult["roundState"]="solve_window";if(r.phase==="playing"){const ends=new Date(now.getTime()+SOLVE_WINDOW_MS);await client.query(`UPDATE public.game_rooms SET first_solver_id=$2,solved_at=$3,solve_window_ends_at=$4,phase='solve_window',state='active',updated_at=clock_timestamp() WHERE id=$1`,[input.roomId,input.playerId,now,ends])}
const result={...resultBase(input,"accepted","accepted"),roundState,cardsConsumed:input.cards.length,cardsDrawn:replacements.length,handChanged:true,handVersion:nextVersion,hand:nextHand,handHash:handHash(nextHand),scoreDelta:1,coinDelta:0};await client.query(`INSERT INTO public.submissions(room_id,player_id,submission_id,round_number,turn_number,submitted_cards,submitted_word,request_hash,status,reason,result) VALUES($1,$2,$3,$4,$5,$6::jsonb,$7,$8,'accepted',$9,$10::jsonb)`,[input.roomId,input.playerId,input.requestId,input.roundNumber,input.turnNumber,JSON.stringify(input.cards),input.word,requestHash,"accepted",JSON.stringify({...result,requestHash})]);const event=await appendEvent(client,input.roomId,input.roundNumber,"word_submitted",{playerId:input.playerId,word,status:"accepted",scoreDelta:1});const snapshot=await readSnapshot(client,input.roomId);await client.query("COMMIT");return{result,events:[event],snapshot};}catch(e){await client.query("ROLLBACK");throw e;}finally{client.release()}}
